<%= form_for(@address_without_postal_code) do |f| %>
  <% if @address_without_postal_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@address_without_postal_code.errors.count, "error") %> prohibited this address_without_postal_code from being saved:</h2>

      <ul>
      <% @address_without_postal_code.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
