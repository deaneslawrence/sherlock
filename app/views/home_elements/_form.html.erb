<%= form_for(@home_element) do |f| %>
  <% if @home_element.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_element.errors.count, "error") %> prohibited this home_element from being saved:</h2>

      <ul>
      <% @home_element.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
